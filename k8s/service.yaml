apiVersion: v1
kind: Service
metadata:
  name: demo-nginx-svc
spec:
  type: LoadBalancer
  selector:
    app: demo-nginx
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP